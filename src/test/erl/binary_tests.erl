%%
%% @author Pavlo Baron <pb@pbit.org>
%% @doc test cases for the Erjang binary module
%%

% to execute it without environment dependency:
% ej -pz <triq-installation>/ebin/ 
% c(ets_test, [{i, "<triq-installation>/include"}]).
% binary_testa:start().

% Other:
% erl -pz ~/OSS/triq/ebin/ -sname erl

-module(binary_tests).

-include("triq.hrl").
